# RUPET Pipeline
# The TIA - Team!

steps:
- script: |
    curl https://api.ipify.org/
  displayName: 'IP info'

- script: |
    echo $(Agent.OS)
  displayName: 'Agent OS'

- task: Xther-Deployment@0
  inputs:
    installDir: '$(Build.SourcesDirectory)/maccli/selenium/'
    resources: 'CHR'
  displayName: 'Xther MAC install'

- task: PowerShell@2
  displayName: 'Running robots'
  inputs:    
    targetType: 'inline'
    script: |
      if ('$(Agent.OS)' -eq 'Darwin')
      {
        chmod 777 '$(Build.SourcesDirectory)/maccli/selenium/chromedriver'
        for ($num = 1; $num -lt $(threads); $num++)
        {
          nohup dotnet run maccli/Gsqa.Tools.AiMaps.Console.dll src='maps/ttDemoRupet.aimj' csv='$(Build.SourcesDirectory)/maps/ttDemoRupet.csv' VarLoadSettings.IterationId='$(iterationId)' VarLoadSettings.ApplicationVersion='$(applicationVersion)' VarLoadSettings.BotName='$(botName)' VarLoadSettings.BotAuthor='$(botAuthor)' &
        }
        dotnet run maccli/Gsqa.Tools.AiMaps.Console.dll src='maps/ttDemoRupet.aimj' csv='$(Build.SourcesDirectory)/maps/ttDemoRupet.csv' VarLoadSettings.IterationId='$(iterationId)' VarLoadSettings.ApplicationVersion='$(applicationVersion)' VarLoadSettings.BotName='$(botName)' VarLoadSettings.BotAuthor='$(botAuthor)'
      }
      elseif ('$(Agent.OS)' -eq 'Windows_NT')
      {
        Write-Host "Windows not implemented"
      }